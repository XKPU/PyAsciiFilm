# PyAsciiFilm

这是一个用于将视频转换为ASCII并在控制台直接播放的Python程序。

**Python 依赖库：**
*   numpy
*   pillow
*   opencv-python

**外部依赖：**
*   需要安装 [FFmpeg](https://ffmpeg.org/)，并确保 `ffplay.exe` 在系统环境变量 `PATH` 中可用（可在下载程序时选择包含ffplay的程序：-with ffplay）
