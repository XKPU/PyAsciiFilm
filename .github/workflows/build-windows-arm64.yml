name: Build Windows ARM64

on:
  workflow_dispatch:
    inputs:
      mode:
        description: "Build mode"
        required: true
        default: standalone
        type: choice
        options: [standalone, onefile]

jobs:
  build-windows-arm64:
    runs-on: windows-11-vs2026-arm
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: requirements.txt
      - run: pip install "nuitka>=2.3.7" ordered-set zstandard
      - run: pip install -r requirements.txt

      - name: Cache Nuitka
        uses: actions/cache@v4
        with:
          path: ${{ runner.temp }}/nuitka-cache-${{ inputs.mode }}
          key: "nuitka-windows_arm64-${{ inputs.mode }}-${{ hashFiles('requirements.txt', 'src/**/*.py') }}"
          restore-keys: |
            "nuitka-windows_arm64-${{ inputs.mode }}-"

      - name: Build
        shell: pwsh
        env:
          NUITKA_CACHE_DIR: "${{ runner.temp }}/nuitka-cache-${{ inputs.mode }}"
        run: pwsh -File build/windows-arm64.ps1 -Mode ${{ inputs.mode }}

      - uses: actions/upload-artifact@v4
        with:
          name: pyasciifilm-windows-arm64${{ inputs.mode == 'onefile' && '-onefile' || '' }}
          path: |
            dist/standalone/*.dist.zip
            dist/onefile/PyAsciiFilm-*-windows_arm64.exe
          if-no-files-found: error
          compression-level: 9
